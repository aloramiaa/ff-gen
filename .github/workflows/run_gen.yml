name: Run Account Generator

on:
  workflow_dispatch:
    inputs:
      region:
        description: 'Target Region (IND, ID, BR, ME, VN, TH, CIS, BD, PK, SG, SAC, TW)'
        required: true
        default: 'IND'
        type: choice
        options:
        - IND
        - ID
        - BR
        - ME
        - VN
        - TH
        - CIS
        - BD
        - PK
        - SG
        - SAC
        - TW
      count:
        description: 'Number of accounts to generate'
        required: true
        default: '100'
        type: string
      name_prefix:
        description: 'Account Name Prefix'
        required: true
        default: 'udhaya'
        type: string
      password_prefix:
        description: 'Password Prefix'
        required: true
        default: 'udhaya'
        type: string
      rarity:
        description: 'Rarity Threshold (1-10)'
        required: false
        default: '4'
        type: string
      threads:
        description: 'Number of Threads'
        required: false
        default: '40'
        type: string
      ghost_mode:
        description: 'Enable Ghost Mode'
        required: false
        default: false
        type: boolean
      firebase_url:
        description: 'Firebase Database URL (e.g. https://your-db.firebaseio.com)'
        required: false
        type: string

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests pycryptodome colorama urllib3 psutil

    - name: Run Generator
      env:
        REGION: ${{ inputs.region }}
        COUNT: ${{ inputs.count }}
        NAME_PREFIX: ${{ inputs.name_prefix }}
        PASS_PREFIX: ${{ inputs.password_prefix }}
        RARITY: ${{ inputs.rarity }}
        THREADS: ${{ inputs.threads }}
        GHOST: ${{ inputs.ghost_mode }}
        FIREBASE_URL: ${{ inputs.firebase_url || secrets.FIREBASE_URL }}
        FIREBASE_SECRET: ${{ secrets.FIREBASE_SECRET }}
      run: |
        REGION_ARG="$REGION"
        if [ "$GHOST" = "true" ]; then
          REGION_ARG="GHOST"
        fi
        
        echo "Running generator for region: $REGION_ARG, count: $COUNT"
        
        # Build command arguments
        CMD_ARGS="--mode cli --region $REGION_ARG --count $COUNT --name-prefix $NAME_PREFIX --password-prefix $PASS_PREFIX --rarity $RARITY --threads $THREADS"
        
        if [ ! -z "$FIREBASE_URL" ] && [ ! -z "$FIREBASE_SECRET" ]; then
          echo "Firebase integration enabled."
          CMD_ARGS="$CMD_ARGS --firebase-url $FIREBASE_URL --firebase-secret $FIREBASE_SECRET"
        fi
        
        python gen.py $CMD_ARGS

    - name: Archive Generated Accounts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: generated-accounts
        path: |
          BIGBULL-ERA/
          !BIGBULL-ERA/**/*.tmpo
        if-no-files-found: warn
